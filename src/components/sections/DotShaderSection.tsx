import DotShaderBackgroundDemo from "@/components/ui/dot-shader-background-demo";

const DotShaderSection = () => {
    return (
        <section className="relative w-full h-screen overflow-hidden">
            <DotShaderBackgroundDemo />
        </section>
    );
};

export default DotShaderSection;
